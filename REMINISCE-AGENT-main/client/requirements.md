## Packages
framer-motion | For smooth animations and transitions, especially critical for the "Help is on the way" effect
date-fns | For formatting dates in the routine and emergency logs
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
